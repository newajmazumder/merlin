import type { CSSProperties } from 'react'

const wrapperStyle: CSSProperties = {
  display: 'contents',
}

export default function PricingCtaLabel() {
  return <span style={wrapperStyle}>Make the switch to Merlin</span>
}
