import type { CSSProperties } from 'react'

const wrapperStyle: CSSProperties = {
  display: 'contents',
}

export default function HeroCtaLabel() {
  return <span style={wrapperStyle}>Start free trial</span>
}
